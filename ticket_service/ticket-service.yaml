apiVersion: v1
kind: Service
metadata:
  name: ticket-service
spec:
  selector:
    app: ticket-service
  serviceAccountName: <your service account name>
  ports:
  - port: 3001
  targetPort: 3001
type: ClusterIP